name: Workspace Smoke

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *' # daily at 03:00 UTC

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Beam
        uses: actions/checkout@v4
      - name: Run Beam reference smoke script
        run: |
          bash scripts/run-workspace-smoke.sh
